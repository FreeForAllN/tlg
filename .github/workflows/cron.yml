name: Schedule PHP Script

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          
      - name: Run PHP Script
        run: 
          php gen_sub.php
          git config --global user.email "thisisman1b@gmail.com"
          git config --global user.name "FreeForAllN"
          git config credential.helper store
          git add -A
          git commit -m "🚀 Config Update - $(date '+%Y-%m-%d %H:%M:%S')"
          git push
      